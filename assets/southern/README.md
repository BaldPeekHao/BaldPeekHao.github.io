# Hoa Vien Phuong Nam demo assets

The portfolio menu intentionally loads its food photography from remote Unsplash image URLs so the portfolio bundle stays lightweight. The demo has a styled fallback background if a photo cannot load.

The original Southern Park source package did not include local menu image files; its menu images are data-driven URLs. No external platform branding is surfaced in the portfolio UI.
